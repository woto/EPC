        # Результаты поиска
        #previous_image = ImageGrab.grab((205, 198, 215, 882))
        #previous_image.save('diff_1.png')

          #print "Getting 10px width area with search result"
          #current_image = ImageGrab.grab((205, 198, 215, 882))
          #diff = ImageChops.difference(previous_image, current_image)
          #if (diff.getbbox() != None):
          #current_image.save('diff_2.png')
          #diff.save('diff.png')